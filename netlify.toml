[build]
  command = "npm run build"
  publish = "dist"

[build.environment]
  NODE_VERSION = "20"
  SECRETS_SCAN_OMIT_PATHS = "supabase/config.toml,.env"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
